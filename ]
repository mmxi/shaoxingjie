- if params[:forum_id]
  - add_breadcrumb "发帖", :new_tforum_path
- else
  - add_breadcrumb "发帖", :new_topic_path
  - add_breadcrumb "发帖", :new_tforum_path

- content_for :breadcrumb do
  = render_bcs

%h2 发布帖子

= render "form"
